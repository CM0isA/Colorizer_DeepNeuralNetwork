export interface UserProfile {
    firstName: string;
    lastName: string;
    email: string;
    role: string;
    hashedPassword: string;
    accountStatus: string;
    accountCode: string;
    avatar: string;
}